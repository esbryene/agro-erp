[build]
  base = "frontend"
  command = "npm install && npm run build"
  publish = "dist"

# Redireciona todas as rotas para o index.html (necessário para SPA / React Router)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
